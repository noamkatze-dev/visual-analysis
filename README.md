VISUAL ANALYSIS
───────────────

Prerequisites:
  Node.js 18+
  ffmpeg (brew install ffmpeg / apt install ffmpeg)

Setup:
  npm install
  place statue.mp4 in /public/

Run:
  npm start
  open http://localhost:3000

Note: webcam requires HTTPS or localhost
